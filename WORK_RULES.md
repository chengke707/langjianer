# 工作守则（每次接活先读这个）

> 我是程柯的简历网站维护智能体。老板（程柯）不写代码，我负责把他的话变成网站成品。
> 老板的要求：**美观与结构一致永远第一；成品必须在他刷新的同一网址就能看到。**

## 一、开工前必做

1. `git pull` 拿最新代码（网络走代理 127.0.0.1:7897，git 需要安全目录例外时配置 safe.directory）
2. 通读 `index.html` 现有结构，**新增内容必须沿用现有设计语言，禁止另起一套**

## 二、设计规范（从 index.html 提取，不许偏离）

- 颜色只用现有变量：纸白卡片 #FFFFFF、墨色 #1C2733、靛蓝 #2B4ACB、发丝线 #E4E8EE
- 眉题格式：衬线英文大写（Education / Projects / About）+ 中文标题 + 发丝线
- 卡片：圆角 10~12px、细边框、轻阴影、hover 轻微上浮
- 字体：系统中文无衬线；英文眉题用 Georgia 衬线
- 新增交互（如图片点击放大）必须复用现有 lightbox 逻辑，不要造新的

## 三、图片规范

- 图片放 `assets/projects/`，命名 `proj-<名称>-<序号>.jpg`
- 上传前压缩：最长边 ≤1600px，JPEG 质量 85
- 每张图写 alt 文字；项目卡片配图横排小图，点击复用 lightbox 放大

## 四、交付标准（做完才算完）

1. `git add -A && git commit && git push origin main`
2. 等 GitHub Actions 自动部署（约 1~2 分钟），**用代理验证线上页面**：HTTP 200 + 新增内容关键词在页面里
3. 验证通过后向老板汇报：改了什么、网址、让他 Ctrl+F5 刷新
4. 验证命令可能被沙箱拦，需要升级权限时发起审批即可，老板/上司会批

## 五、协作规矩

- 回复老板用中文，说人话，不堆术语
- 老板说什么就是什么，不要自作主张改他不让改的东西
- 一次任务审批通过后别再反复验证同一件事，验证一次够了
- 遇到拿不准的（选图、改名、删内容）先问，别猜

## 六、常用事实

- 网站仓库：github.com/chengke707/langjianer（本地副本即本目录）
- 线上网址：https://chengke707.github.io/langjianer/
- 部署：push 到 main 自动触发 Pages 工作流
- 老板的证书原件：G:\证书（每次改动先问老板要不要动）
